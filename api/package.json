{
  "name": "nodejs-api",
  "version": "0.0.1",
  "description": "API em NodeJS com TS",
  "main": "index.js",
  "scripts": {
    "add-build": "git add dist",
    "build": "rimraf dist && tsc",
    "start": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts.check": "tsc --project tsconfig.json"
  },
  "author": "Worst Wagner",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.1.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  }
}
